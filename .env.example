# API origin without /api (optional; production defaults to the current origin)
VITE_API_URL=http://localhost:5002

# WebSocket origin (optional; defaults to the API origin)
VITE_SOCKET_URL=http://localhost:5002
